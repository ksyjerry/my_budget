# S3 — Step 1/Step 3 UX 개선 + AI 엔드포인트 안정화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자 피드백 #15, #17, #18, #29, #30, #31, #36, #37, #38, #42, #56 대응. Step 1 NumberField 개선 + ET Controllable 라벨·툴팁·travel 분리 + Project 검색 autofill 확장 + Step 3 버튼 레이아웃/중복 key 수정 + AI 엔드포인트 에러 개선 + QRP 수기 안내 + 기존 기능(autofill, 임시저장) 회귀 테스트.

**Architecture:**
- **백엔드 1 파일:** `budget_assist.py` — AI 엔드포인트 에러 코드/메시지 개선 (config 없으면 503 + 명확 메시지)
- **프론트 1 파일:** `budget-input/[project_code]/page.tsx` — NumberField 개선, ET Controllable 라벨/툴팁/travel UI 분리, ProjectSearch autofill, Step 3 버튼 레이아웃, React key 수정, AI 에러 detail 표시, QRP placeholder
- **테스트:** 백엔드 pytest 1 파일 (budget_assist), Playwright 2 specs (autofill, number field)

**Tech Stack:** FastAPI, Next.js 16, React, Playwright, pytest.

**Spec:** [docs/superpowers/specs/2026-04-21-s3-step1-ux-design.md](../specs/2026-04-21-s3-step1-ux-design.md)

---

## Task 1: AI 엔드포인트 에러 코드/메시지 개선 (#18, #42)

**Files:**
- Modify: `backend/app/api/v1/budget_assist.py`
- Create: `backend/tests/test_budget_assist_errors.py`

### Step 1: 조사 — 현재 실패 시나리오

```bash
cd /Users/jkim564/Documents/Programming/my_budget/.worktrees/s3-step1-ux/backend
python -c "
from app.api.v1.budget_assist import _get_client
try:
    c = _get_client()
    print('client ok:', c)
except Exception as e:
    print('error:', type(e).__name__, str(e))
"
```

Expected: 하나의 3가지 결과
- `error: HTTPException 'GenAI Gateway 설정이 없습니다.'` → config 미설정이 primary 원인
- `client ok: <obj>` → config 는 있음, 실행 시점 실패는 다른 이유
- 다른 에러 → 즉시 보고

기록된 결과를 참고해 Step 2 진행.

### Step 2: 실패 테스트 작성

Create `backend/tests/test_budget_assist_errors.py`:

```python
"""Tests for budget_assist endpoint error handling (#18, #42)."""
from unittest.mock import patch

import pytest


def test_suggest_returns_503_when_config_missing(client, elpm_cookie, monkeypatch):
    """#18 #42 — config 없으면 503 (not 500) 과 사용자 친화 메시지."""
    monkeypatch.delenv("GENAI_BASE_URL", raising=False)
    monkeypatch.delenv("PwC_LLM_API_KEY", raising=False)
    r = client.post(
        "/api/v1/budget-assist/suggest",
        json={
            "project_code": "TEST",
            "et_controllable": 100.0,
            "enabled_units": [{"category": "자산", "unit_name": "매출채권-일반"}],
            "members": [{"empno": "170661", "name": "최성우", "grade": "EL"}],
        },
        cookies=elpm_cookie,
    )
    assert r.status_code == 503, r.text
    body = r.json()
    assert "detail" in body
    # 사용자 친화적 메시지 — "설정", "AI", "서비스", "관리자" 중 하나 포함
    detail_lower = body["detail"].lower()
    assert any(kw in body["detail"] for kw in ["AI", "설정", "관리자", "서비스"])


def test_validate_returns_503_when_config_missing(client, elpm_cookie, monkeypatch):
    monkeypatch.delenv("GENAI_BASE_URL", raising=False)
    monkeypatch.delenv("PwC_LLM_API_KEY", raising=False)
    r = client.post(
        "/api/v1/budget-assist/validate",
        json={
            "project_code": "TEST",
            "et_controllable": 100.0,
            "rows": [],
        },
        cookies=elpm_cookie,
    )
    assert r.status_code == 503, r.text
    assert "detail" in r.json()


def test_suggest_returns_502_when_llm_raises(client, elpm_cookie, monkeypatch):
    """외부 LLM 호출 실패 시 502 + 구체 detail."""
    # Config 는 있다고 설정
    monkeypatch.setenv("GENAI_BASE_URL", "http://fake.example.com")
    monkeypatch.setenv("PwC_LLM_API_KEY", "test-key")

    from app.api.v1 import budget_assist

    class FakeClient:
        async def complete_json(self, **kwargs):
            raise RuntimeError("simulated LLM network failure")

    with patch.object(budget_assist, "_get_client", return_value=FakeClient()):
        r = client.post(
            "/api/v1/budget-assist/suggest",
            json={
                "project_code": "TEST",
                "et_controllable": 100.0,
                "enabled_units": [{"category": "자산", "unit_name": "매출채권-일반"}],
                "members": [{"empno": "170661", "name": "최성우", "grade": "EL"}],
            },
            cookies=elpm_cookie,
        )
    assert r.status_code == 502, r.text
    detail = r.json().get("detail", "")
    assert "AI" in detail or "실패" in detail
```

### Step 3: 실행 — 실패 확인

```bash
cd backend && pytest tests/test_budget_assist_errors.py -v
```

Expected: 모두 실패 — 현재는 503 이 아닌 500 반환.

### Step 4: `_get_client` 수정 — 503 + 명확한 메시지

In `backend/app/api/v1/budget_assist.py`, find `_get_client` function (around line 140). Replace:

Before:
```python
    if not base_url or not api_key:
        raise HTTPException(status_code=500, detail="GenAI Gateway 설정이 없습니다.")
```

After:
```python
    if not base_url or not api_key:
        raise HTTPException(
            status_code=503,
            detail="AI 서비스가 현재 구성되지 않았습니다. 관리자에게 문의해주세요. (PwC_LLM_API_KEY 또는 GENAI_BASE_URL 미설정)",
        )
```

### Step 5: `/suggest` / `/validate` 의 LLM 호출 실패 메시지 개선

In `backend/app/api/v1/budget_assist.py`, find the `try/except` blocks around the LLM call (around line 217-226 for suggest; similar pattern for validate).

Replace:

```python
    except Exception as e:
        logger.error(f"Budget suggest LLM error: {e}")
        raise HTTPException(status_code=502, detail="AI 추천 생성에 실패했습니다.")
```

With:

```python
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"Budget suggest LLM error: {e}", exc_info=True)
        raise HTTPException(
            status_code=502,
            detail=f"AI 추천 생성에 실패했습니다. 잠시 후 다시 시도해주세요. ({type(e).__name__})",
        )
```

Same change for `/validate` endpoint.

### Step 6: 실행 — 테스트 통과

```bash
cd backend && pytest tests/test_budget_assist_errors.py -v
```

Expected: 3 passed.

### Step 7: 전체 pytest

```bash
cd backend && pytest -q 2>&1 | tail -3
```

Expected: 81 + 3 = 84 passed.

### Step 8: Commit

```bash
git add backend/app/api/v1/budget_assist.py backend/tests/test_budget_assist_errors.py
git commit -m "fix(s3): budget_assist returns 503/502 with user-friendly detail (#18 #42)"
```

---

## Task 2: NumberField 개선 — min=0, 천단위, 음수 방지 (#38)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: NumberField Props 확장

Find the `NumberField` component (around lines 44-54). Replace with extended version:

```tsx
interface NumberFieldProps {
  label: string;
  value?: number;
  onChange?: (v: number) => void;
  readOnly?: boolean;
  step?: number;
  min?: number;
  allowNegative?: boolean;
}

function NumberField(props: NumberFieldProps) {
  const {
    label, value, onChange, readOnly, step = 1,
    min, allowNegative = false,
  } = props;

  const display =
    readOnly && typeof value === "number"
      ? value.toLocaleString("ko-KR")
      : value ?? "";

  return (
    <div className="flex flex-col gap-1">
      <label className="text-xs font-medium text-pwc-gray-600">{label}</label>
      <input
        type={readOnly ? "text" : "number"}
        value={display}
        step={step}
        min={min}
        readOnly={readOnly}
        onChange={(e) => {
          let v = parseFloat(e.target.value);
          if (Number.isNaN(v)) v = 0;
          if (!allowNegative && v < 0) v = 0;
          if (typeof min === "number" && v < min) v = min;
          onChange?.(v);
        }}
        className={
          "px-2 py-1.5 border border-pwc-gray-200 rounded-md text-sm " +
          (readOnly
            ? "bg-pwc-gray-50 text-pwc-gray-900"
            : "bg-white focus:outline-none focus:border-pwc-orange")
        }
      />
    </div>
  );
}
```

### Step 2: 시간 배분 input 들에 `min={0}` 추가

Find each `<NumberField>` in the "시간 배분" section (axdx, qrp, rm, el, pm, ra_elpm, fulcrum, ra_staff, specialist, travel — roughly lines ~1460-1560). Add `min={0}` prop to each:

Before (example):
```tsx
<NumberField
  label="AX/DX 시간"
  value={project.axdx_hours}
  onChange={(v) => pField("axdx_hours", v)}
/>
```

After:
```tsx
<NumberField
  label="AX/DX 시간"
  value={project.axdx_hours}
  onChange={(v) => pField("axdx_hours", v)}
  min={0}
/>
```

Apply to all 10+ time-distribution NumberFields.

### Step 3: TypeScript + build

```bash
cd frontend && npx tsc --noEmit 2>&1 | grep -E "page\.tsx|NumberField" | head -5
cd frontend && npm run build 2>&1 | tail -5
```

Must succeed.

### Step 4: Commit

```bash
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s3): NumberField supports min/allowNegative + comma display readOnly (#38)"
```

---

## Task 3: ET Controllable 라벨·툴팁·travel 분리 (#30, #31, #38 경고)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: ET controllable 라벨 변경

Find the NumberField for ET controllable (around lines 1543-1547). Change the label from "ET Controllable Budget" to "ET 잔여 시간 (Controllable Budget)".

Before:
```tsx
<NumberField
  label="ET Controllable Budget"
  value={etControllable}
  readOnly
/>
```

After:
```tsx
<div className="flex flex-col gap-1">
  <div className="flex items-center gap-1 text-xs font-medium text-pwc-gray-600">
    ET 잔여 시간 (Controllable Budget)
    <span
      className="cursor-help text-pwc-gray-400"
      title={
        "총 계약시간 − (AX/DX + QRP + RM/CRS/M&T + FLDT-EL + FLDT-PM + " +
        "RA-EL/PM + Fulcrum + RA-Staff + Specialist + 출장시간)\n\n" +
        "= FLDT 구성원이 집행할 수 있는 실제 Budget 시간"
      }
    >
      ⓘ
    </span>
  </div>
  <input
    type="text"
    value={etControllable.toLocaleString("ko-KR")}
    readOnly
    className={
      "px-2 py-1.5 border rounded-md text-sm bg-pwc-gray-50 " +
      (etControllable < 0 ? "border-pwc-red text-pwc-red" : "border-pwc-gray-200 text-pwc-gray-900")
    }
  />
  {etControllable < 0 && (
    <p className="text-xs text-pwc-red">
      ⚠ 차감 항목 합계가 총 계약시간을 초과합니다. 시간 배분을 재검토하세요.
    </p>
  )}
</div>
```

### Step 2: travel_hours 시각 분리 (#30)

Find the "시간 배분" grid. Currently all NumberFields are in one grid. The `travel_hours` NumberField should be moved to a separate visual group.

Locate the `travel_hours` NumberField (around line 1555). Wrap the existing 팀 구성원별 NumberFields and the travel NumberField in two separate sections:

Before (approximate):
```tsx
<div className="grid grid-cols-4 gap-3">
  <NumberField label="AX/DX 시간" ... />
  <NumberField label="QRP 시간" ... />
  ...
  <NumberField label="Specialist 시간" ... />
  <NumberField label="출장시간" value={project.travel_hours} ... />
</div>
```

After:
```tsx
{/* Group A: 팀 구성원별 시간 */}
<div className="mb-4">
  <h4 className="text-xs font-semibold text-pwc-gray-900 mb-2">팀 구성원별 시간</h4>
  <div className="grid grid-cols-4 gap-3">
    <NumberField label="AX/DX 시간" ... />
    <NumberField label="QRP 시간" ... />
    <NumberField label="RM/CRS/M&T 시간" ... />
    <NumberField label="FLDT-EL 시간" ... />
    <NumberField label="FLDT-PM 시간" ... />
    <NumberField label="RA-EL/PM 시간" ... />
    <NumberField label="Fulcrum 시간" ... />
    <NumberField label="RA-Staff 시간" ... />
    <NumberField label="Specialist 시간" ... />
  </div>
</div>

{/* Group B: 기타 차감 항목 */}
<div className="mb-4">
  <h4 className="text-xs font-semibold text-pwc-gray-900 mb-2">기타 차감 항목</h4>
  <div className="grid grid-cols-4 gap-3">
    <NumberField
      label="출장시간"
      value={project.travel_hours}
      onChange={(v) => pField("travel_hours", v)}
      min={0}
    />
  </div>
  <p className="text-xs text-pwc-gray-600 mt-1">
    * 출장시간도 ET 잔여 시간에서 차감됩니다.
  </p>
</div>
```

Replace actual JSX incrementally — match the exact field list from the existing code (prop names may vary slightly).

### Step 3: Build check

```bash
cd frontend && npm run build 2>&1 | tail -5
```

### Step 4: Commit

```bash
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s3): ET Controllable tooltip/label + travel_hours separated (#30 #31 #38)"
```

---

## Task 4: ProjectSearch autofill — 클라이언트 정보 호출 (#37)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: ProjectSearchModal onSelect 확장

Find the ProjectSearchModal `onSelect={...}` block (around lines 1315-1349). Currently it only sets project fields.

Add an async autofill step: after project fields are set, if `p.client_code` exists, fetch `/clients/{code}/info` and merge into client state. Reuse the same logic pattern from ClientSearchModal's onSelect (already exists in this file per S1 T11).

Before (approximate):
```tsx
onSelect={(p) => {
  setProject({...});
  if (p.client_code) {
    cField("client_code", p.client_code as string);
  }
}}
```

After:
```tsx
onSelect={async (p) => {
  setProject({
    ...project,
    project_code: p.project_code as string,
    project_name: p.project_name as string,
    // ... (existing field mappings — preserve all)
  });
  if (p.client_code) {
    const code = p.client_code as string;
    cField("client_code", code);
    try {
      const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:3001";
      const r = await fetch(`${API_BASE}/api/v1/budget/clients/${code}/info`, {
        credentials: "include",
      });
      if (r.ok) {
        const info = await r.json();
        setClient((prev) => ({
          ...prev,
          industry: prev.industry || info.industry || "",
          asset_size: prev.asset_size || info.asset_size || "",
          listing_status: prev.listing_status || info.listing_status || "",
          business_report: prev.business_report || info.business_report || "",
          gaap: prev.gaap || info.gaap || "",
          consolidated: prev.consolidated || info.consolidated || "",
          subsidiary_count: prev.subsidiary_count || info.subsidiary_count || "",
          internal_control: prev.internal_control || info.internal_control || "",
          initial_audit: prev.initial_audit || info.initial_audit || "",
        }));
      }
    } catch {
      /* silent fail */
    }
  }
}}
```

**Note:** If `setClient` is typed as `(c: ClientInfo) => void` (not functional setter), capture `client` at call time instead of using `prev`:

```tsx
const base = client;  // snapshot before async
// ...
setClient({
  ...base,
  industry: base.industry || info.industry || "",
  ...
});
```

### Step 2: Build check

```bash
cd frontend && npm run build 2>&1 | tail -5
```

### Step 3: Commit

```bash
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s3): ProjectSearch onSelect auto-fills client info (#37)"
```

---

## Task 5: Step 3 AI 버튼 / 다음 버튼 레이아웃 (#15) + React key (#17)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: 버튼 레이아웃 조사

```bash
cd frontend && grep -n "AI 추천\|AI 검증\|다음\|이전" src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx | head -10
```

Identify the AI buttons' container and the navigation (이전/다음) buttons' container. Record line numbers.

### Step 2: AI 버튼 container 재작성 (#15)

Find the AI buttons section (around line 2160). Replace with a flex-wrap container:

Before (example):
```tsx
<div>
  <button onClick={handleAISuggest}>AI 추천</button>
  <button onClick={handleAIValidate}>AI 검증</button>
</div>
```

After:
```tsx
<div className="flex flex-wrap gap-2 items-center mb-3">
  <button
    onClick={handleAISuggest}
    disabled={aiLoading}
    className="px-3 py-1.5 text-xs border border-pwc-orange text-pwc-orange rounded-md hover:bg-pwc-orange hover:text-white disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
  >
    AI 추천
  </button>
  <button
    onClick={handleAIValidate}
    disabled={aiLoading}
    className="px-3 py-1.5 text-xs border border-pwc-orange text-pwc-orange rounded-md hover:bg-pwc-orange hover:text-white disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
  >
    AI 검증
  </button>
</div>
```

Retain existing aiLoading / handleAISuggest / handleAIValidate logic.

### Step 3: Step 3 table 하단 padding 증가

Find the Step 3 table container (wrap around the table component). Add `pb-24` class so the last row doesn't hide behind the sticky navigation buttons:

```tsx
<div className="overflow-x-auto pb-24">
  <table>...</table>
</div>
```

### Step 4: 다음 버튼 container z-index

Find the sticky bottom navigation buttons. Add `z-10`:

```tsx
<div className="sticky bottom-0 bg-white border-t border-pwc-gray-100 py-3 px-4 flex gap-2 justify-end z-10">
  <button>이전</button>
  <button>다음</button>
</div>
```

### Step 5: #17 duplicate key 수정

```bash
cd frontend && grep -n "MONTHS\.map\|rows\.map\|\.map((row\|\.map((m" src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx | head -10
```

Identify map calls using `key` prop. For each, verify keys are unique. Common fixes:

**For MONTHS.map:**
```tsx
{MONTHS.map((month, idx) => (
  <td key={`${idx}-${month}`}>  {/* was key={month} */}
    ...
  </td>
))}
```

**For row.map:**
```tsx
{rows.map((row, idx) => (
  <tr key={row.id ?? `${row.budget_unit ?? ""}-${idx}`}>  {/* was key={row.budget_unit} */}
    ...
  </tr>
))}
```

Apply the pattern wherever keys may collide. Open browser console after build and verify no "duplicate key" warning.

### Step 6: Build check + manual console check

```bash
cd frontend && npm run build 2>&1 | tail -5
```

### Step 7: Commit

```bash
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "fix(s3): Step3 button layout + duplicate key React warning (#15 #17)"
```

---

## Task 6: AI 에러 detail 표시 (#18, #42 프론트)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: AI 호출 함수 찾기

```bash
cd frontend && grep -n "budget-assist\|aiLoading\|handleAISuggest\|handleAIValidate" src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx | head -10
```

Identify the two handlers (suggest + validate).

### Step 2: 에러 detail 표시

For each handler (around lines 1914-1930 for suggest, 1940-1964 for validate), find:

Before (example):
```tsx
const r = await fetch(`${API_BASE}/api/v1/budget-assist/suggest`, {...});
if (!r.ok) {
  alert("AI 추천 실패");
  return;
}
```

After:
```tsx
const r = await fetch(`${API_BASE}/api/v1/budget-assist/suggest`, {...});
if (!r.ok) {
  const data = await r.json().catch(() => ({ detail: "AI 추천 실패" }));
  alert(data.detail || "AI 추천 실패");
  return;
}
```

Apply to both suggest AND validate handlers.

### Step 3: Build check

```bash
cd frontend && npm run build 2>&1 | tail -5
```

### Step 4: Commit

```bash
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s3): surface AI endpoint error detail in alert (#18 #42)"
```

---

## Task 7: QRP placeholder / 수기 안내 (#36)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: QRP NumberField 에 placeholder / label 업데이트

Find the QRP empno input field and QRP hours NumberField (around lines 1489-1494). Update the label to hint that manual entry is OK:

For the hours field:
```tsx
<NumberField
  label="QRP 시간 (수기 입력 가능)"
  value={project.qrp_hours}
  onChange={(v) => pField("qrp_hours", v)}
  min={0}
/>
```

For the QRP empno text input (if separate), add a placeholder:
```tsx
<input
  type="text"
  placeholder="사번 입력 또는 검색"
  value={project.qrp_empno ?? ""}
  onChange={(e) => pField("qrp_empno", e.target.value)}
  className="..."
/>
```

### Step 2: Build + commit

```bash
cd frontend && npm run build 2>&1 | tail -3
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s3): QRP field label/placeholder indicates manual entry OK (#36)"
```

---

## Task 8: Playwright E2E (#37, #38 검증)

**Files:**
- Create: `frontend/tests/task-s3-project-search-autofill.spec.ts`
- Create: `frontend/tests/task-s3-number-field-validation.spec.ts`

### Step 1: Project search autofill test (API-level)

Create `frontend/tests/task-s3-project-search-autofill.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

const API = "http://localhost:3001/api/v1";
const EL = process.env.EL_EMPNO || "170661";

test.describe("S3 — Project search autofill (#37)", () => {
  test("/clients/{code}/info returns 11 client fields", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    const search = await request.get(`${API}/budget/clients/search?q=`);
    const clients = await search.json();
    if (!Array.isArray(clients) || clients.length === 0) {
      test.skip();
      return;
    }
    const code = clients[0].client_code;
    const r = await request.get(`${API}/budget/clients/${code}/info`);
    expect(r.status()).toBe(200);
    const info = await r.json();
    for (const key of [
      "client_code", "client_name", "industry", "asset_size",
      "listing_status", "business_report", "gaap", "consolidated",
      "subsidiary_count", "internal_control", "initial_audit",
    ]) {
      expect(info).toHaveProperty(key);
    }
  });

  test("project search returns results with client_code", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    const r = await request.get(`${API}/budget/projects/search?q=`);
    expect(r.status()).toBe(200);
    const projects = await r.json();
    if (Array.isArray(projects) && projects.length > 0) {
      // 대부분 프로젝트는 client_code 가 있어야 함
      const withClient = projects.filter((p: { client_code?: string }) => p.client_code);
      expect(withClient.length).toBeGreaterThan(0);
    }
  });
});
```

### Step 2: NumberField + AI error spec

Create `frontend/tests/task-s3-number-field-validation.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

const API = "http://localhost:3001/api/v1";
const EL = process.env.EL_EMPNO || "170661";

test.describe("S3 — AI endpoint error shape (#18 #42)", () => {
  test("suggest without config returns 503 with detail", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    const r = await request.post(`${API}/budget-assist/suggest`, {
      data: {
        project_code: "NONEXISTENT",
        et_controllable: 100.0,
        enabled_units: [{ category: "자산", unit_name: "매출채권-일반" }],
        members: [{ empno: EL, name: "최성우", grade: "EL" }],
      },
    });
    // config 미설정 시 503, 실제 LLM 호출되면 502 또는 200
    expect([200, 502, 503]).toContain(r.status());
    if (r.status() !== 200) {
      const body = await r.json();
      expect(body).toHaveProperty("detail");
      expect(typeof body.detail).toBe("string");
      expect(body.detail.length).toBeGreaterThan(0);
    }
  });

  test("validate returns recognizable error shape on config miss", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    const r = await request.post(`${API}/budget-assist/validate`, {
      data: {
        project_code: "NONEXISTENT",
        et_controllable: 100.0,
        rows: [],
      },
    });
    expect([200, 502, 503]).toContain(r.status());
    if (r.status() !== 200) {
      const body = await r.json();
      expect(body).toHaveProperty("detail");
    }
  });
});
```

### Step 3: Run specs

```bash
cd frontend && npx playwright test task-s3 --reporter=line 2>&1 | tail -10
```

Expected: 4 passed (2 autofill + 2 AI error).

### Step 4: Commit

```bash
git add frontend/tests/task-s3-project-search-autofill.spec.ts frontend/tests/task-s3-number-field-validation.spec.ts
git commit -m "test(s3): Playwright E2E for project autofill + AI error shape"
```

---

## Task 9: Final verification

**Files:** 없음 (verification only)

### Step 1: Full backend pytest

```bash
cd backend && pytest -q 2>&1 | tail -3
```

Expected: 84 passed.

### Step 2: Full Playwright (S0 + S1 + S2 + S3)

```bash
cd frontend && npx playwright test task-auth task-s1 task-s2 task-s3 --reporter=line 2>&1 | tail -10
```

Expected: 대부분 pass.

### Step 3: Commit history

```bash
cd /Users/jkim564/Documents/Programming/my_budget/.worktrees/s3-step1-ux && git log main..HEAD --oneline
```

Expected: 7-8 commits tagged `feat(s3)` / `fix(s3)` / `test(s3)`.

### Step 4: Feedback mapping summary

| # | Status | Commit |
|---|---|---|
| #15 | Step 3 button layout (Task 5) | `fix(s3) ...` |
| #17 | React duplicate key fix (Task 5) | same commit |
| #18 | AI error 503/502 + detail (Task 1 + 6) | `fix(s3) ...` |
| #29 | Already in S1 T11 (no new commit) | — |
| #30 | travel_hours UI separation (Task 3) | `feat(s3) ...` |
| #31 | ET Controllable label + tooltip (Task 3) | same |
| #36 | QRP placeholder (Task 7) | `feat(s3) ...` |
| #37 | ProjectSearch autofill (Task 4) | `feat(s3) ...` |
| #38 | NumberField min/comma (Task 2) | `feat(s3) ...` |
| #42 | Same as #18 | — |
| #56 | Already exists (임시저장 button) | — |

### Step 5: Manual verification (handed to user)

- Step 1 에서 음수 시간 입력 불가
- ET Controllable 에 ⓘ 툴팁 표시
- travel_hours 가 "기타 차감 항목" 섹션에 분리
- Project 검색만으로 클라이언트 9개 필드 자동 채워짐
- Step 3 AI/다음 버튼 겹치지 않음
- Console 에 duplicate key 경고 없음
- AI 버튼 클릭 시 구체 에러 메시지 표시 (config 없으면 "AI 서비스가 구성되지 않았습니다...")
- QRP 필드 placeholder "수기 입력 가능" 문구

---

## 완료 기준

- [ ] Task 1-8 모두 완료
- [ ] pytest ≥ 84 passed
- [ ] Playwright S0+S1+S2+S3 통과
- [ ] 11 feedback items 매핑 완료 (9건 신규 + 2건 회귀 확인)
