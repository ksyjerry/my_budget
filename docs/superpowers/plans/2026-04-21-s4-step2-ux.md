# S4 — Step 2 UX 개선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자 피드백 #39/#40/#46/#54 대응. 퇴사/휴직 empno 검증 + Step 2 구성원 Excel 업/다운로드 + "이전" 단계 이동 시 상태 보존 + Enter 키 자동완성.

**Architecture:**
- **백엔드 1 파일:** `budget_input.py` — employees search emp_status 필터, 신규 upload/export 엔드포인트
- **프론트 1 파일:** `budget-input/[project_code]/page.tsx` — EmployeeSearch onKeyDown, empno 검증 alert, Step 2 import/export UI, 이전 단계 state 보존
- **테스트:** pytest 2 파일, Playwright 3 specs

**Tech Stack:** FastAPI, openpyxl, Next.js 16, React, Playwright, pytest.

**Spec:** [docs/superpowers/specs/2026-04-21-s4-step2-ux-design.md](../specs/2026-04-21-s4-step2-ux-design.md)

---

## Task 1: `/employees/search` 퇴사/휴직 필터 + emp_status 응답 (#39 백엔드)

**Files:**
- Modify: `backend/app/api/v1/budget_input.py` (search_employees function)
- Create: `backend/tests/test_employees_search_filter.py`

### Step 1: 실패 테스트 작성

Create `backend/tests/test_employees_search_filter.py`:

```python
"""Tests for /employees/search active-only filter (#39)."""
import pytest

from app.db.session import SessionLocal
from app.models.employee import Employee


@pytest.fixture(autouse=True)
def _seed_employees():
    """Ensure at least one active and one inactive employee exist."""
    s = SessionLocal()
    try:
        if s.query(Employee).filter(Employee.empno == "S4AC1").first() is None:
            s.add(Employee(empno="S4AC1", name="재직테스트", emp_status="재직"))
        if s.query(Employee).filter(Employee.empno == "S4IN1").first() is None:
            s.add(Employee(empno="S4IN1", name="퇴사테스트", emp_status="퇴사"))
        s.commit()
    finally:
        s.close()
    yield


def test_search_returns_emp_status_field(client, elpm_cookie):
    """응답 dict 에 emp_status 필드가 있어야 함."""
    r = client.get("/api/v1/budget/employees/search?q=재직", cookies=elpm_cookie)
    assert r.status_code == 200
    rows = r.json()
    if rows:
        assert "emp_status" in rows[0]


def test_search_excludes_inactive_by_default(client, elpm_cookie):
    """기본적으로 재직자만 반환."""
    r = client.get("/api/v1/budget/employees/search?q=S4IN1", cookies=elpm_cookie)
    assert r.status_code == 200
    rows = r.json()
    assert all(row.get("emp_status", "") == "재직" for row in rows)


def test_search_includes_active_employee(client, elpm_cookie):
    r = client.get("/api/v1/budget/employees/search?q=S4AC1", cookies=elpm_cookie)
    assert r.status_code == 200
    rows = r.json()
    matching = [r for r in rows if r["empno"] == "S4AC1"]
    assert len(matching) == 1
    assert matching[0]["emp_status"] == "재직"
```

### Step 2: 실행 — 실패 확인

```bash
cd backend && pytest tests/test_employees_search_filter.py -v
```

Expected: `test_search_returns_emp_status_field` 실패 (응답에 emp_status 키 없음), `test_search_excludes_inactive_by_default` 는 DB 상태에 따라 다름.

### Step 3: `search_employees` 수정

In `backend/app/api/v1/budget_input.py`, find the `search_employees` function (around line 149). It queries the Employee model.

Current rough structure:
```python
@router.get("/employees/search")
def search_employees(q: str = "", ..., db: Session = Depends(get_db)):
    query = db.query(Employee)
    if q:
        query = query.filter((Employee.empno.ilike(f"%{q}%")) | (Employee.name.ilike(f"%{q}%")))
    rows = query.limit(...).all()
    return [{"empno": r.empno, "name": r.name, "grade_name": r.grade_name, "department": r.department} for r in rows]
```

Update to:

```python
@router.get("/employees/search")
def search_employees(
    q: str = "",
    include_inactive: bool = False,
    user: dict = Depends(require_login),
    db: Session = Depends(get_db),
):
    query = db.query(Employee)
    if not include_inactive:
        query = query.filter(Employee.emp_status == "재직")
    if q:
        query = query.filter(
            (Employee.empno.ilike(f"%{q}%")) | (Employee.name.ilike(f"%{q}%"))
        )
    rows = query.limit(50).all()
    return [
        {
            "empno": r.empno,
            "name": r.name,
            "grade_name": r.grade_name,
            "department": r.department,
            "emp_status": r.emp_status,
        }
        for r in rows
    ]
```

**Note:** Check the existing function's exact signature — preserve any other existing parameters. Only add `include_inactive` and the filter/response fields.

### Step 4: 테스트 재실행

```bash
cd backend && pytest tests/test_employees_search_filter.py -v
```

Expected: 3 passed.

### Step 5: Full regression

```bash
cd backend && pytest -q 2>&1 | tail -3
```

Expected: baseline + 3 = baseline+3 passed.

### Step 6: Commit

```bash
git add backend/app/api/v1/budget_input.py backend/tests/test_employees_search_filter.py
git commit -m "feat(s4): /employees/search filters active + returns emp_status (#39)"
```

---

## Task 2: Step 2 Excel import/export 엔드포인트 (#40 백엔드)

**Files:**
- Modify: `backend/app/api/v1/budget_input.py` (add 2 endpoints)
- Create: `backend/tests/test_members_upload_export.py`

### Step 1: Export 엔드포인트 추가

In `backend/app/api/v1/budget_input.py`, after the existing member-related endpoints (upsert, list), add:

```python
import io
from fastapi import UploadFile, File
from fastapi.responses import StreamingResponse


@router.get("/projects/{project_code}/members/export")
def export_project_members(
    project_code: str,
    user: dict = Depends(require_login),
    db: Session = Depends(get_db),
):
    """Step 2 구성원 목록 Excel 다운로드."""
    from openpyxl import Workbook
    from app.models.budget_master import ProjectMember

    members = (
        db.query(ProjectMember)
        .filter(ProjectMember.project_code == project_code)
        .order_by(ProjectMember.sort_order, ProjectMember.id)
        .all()
    )

    wb = Workbook()
    ws = wb.active
    ws.title = "구성원"
    ws.append(["empno", "name", "role", "grade"])
    for m in members:
        ws.append([m.empno or "", m.name or "", m.role or "FLDT", m.grade or ""])

    buf = io.BytesIO()
    wb.save(buf)
    buf.seek(0)
    return StreamingResponse(
        buf,
        media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={
            "Content-Disposition": f'attachment; filename="members_{project_code}.xlsx"',
        },
    )


@router.post("/projects/{project_code}/members/upload")
async def upload_project_members(
    project_code: str,
    file: UploadFile = File(...),
    user: dict = Depends(require_elpm),
    db: Session = Depends(get_db),
):
    """Step 2 구성원 목록 Excel 업로드 — FLDT 구성원 truncate+insert."""
    from openpyxl import load_workbook
    from app.models.budget_master import ProjectMember
    from app.models.employee import Employee
    from app.api.deps import assert_can_modify_project

    assert_can_modify_project(db, user, project_code)

    content = await file.read()
    wb = load_workbook(io.BytesIO(content), data_only=True)
    ws = wb.active

    rows = list(ws.iter_rows(min_row=2, values_only=True))  # skip header

    # FLDT 구성원만 truncate (지원 구성원 Fulcrum/RA/Specialist 보존)
    db.query(ProjectMember).filter(
        ProjectMember.project_code == project_code,
        ProjectMember.role == "FLDT",
    ).delete(synchronize_session=False)
    db.commit()

    imported: list[dict] = []
    skipped: list[dict] = []
    for idx, row in enumerate(rows):
        if not row or not any(c is not None for c in row):
            continue
        empno = str(row[0]).strip() if row[0] else ""
        name = str(row[1]).strip() if len(row) > 1 and row[1] else ""
        role = str(row[2]).strip() if len(row) > 2 and row[2] else "FLDT"
        grade = str(row[3]).strip() if len(row) > 3 and row[3] else ""

        if not empno:
            skipped.append({"row": idx + 2, "reason": "empno missing"})
            continue

        emp = db.query(Employee).filter(Employee.empno == empno).first()
        if emp is None:
            skipped.append({"empno": empno, "reason": "not_found"})
            continue
        if emp.emp_status and emp.emp_status != "재직":
            skipped.append({"empno": empno, "reason": "inactive"})
            continue

        db.add(ProjectMember(
            project_code=project_code,
            role=role,
            name=name or emp.name,
            empno=empno,
            grade=grade or (emp.grade_name or ""),
            sort_order=idx,
        ))
        imported.append({"empno": empno, "name": name or emp.name})

    db.commit()
    return {
        "imported_count": len(imported),
        "imported": imported,
        "skipped": skipped,
    }
```

### Step 2: 테스트

Create `backend/tests/test_members_upload_export.py`:

```python
"""Tests for Step 2 members Excel export/upload (#40)."""
import io

import pytest
from openpyxl import Workbook, load_workbook

from app.db.session import SessionLocal
from app.models.budget_master import ProjectMember
from app.models.project import Project
from app.models.employee import Employee


TEST_PROJECT_CODE = "S4_TEST_MEM"


@pytest.fixture(autouse=True)
def _seed():
    s = SessionLocal()
    try:
        # Project
        if s.query(Project).filter(Project.project_code == TEST_PROJECT_CODE).first() is None:
            s.add(Project(
                project_code=TEST_PROJECT_CODE,
                project_name="S4 Members Test",
                el_empno="170661", pm_empno="170661",
                contract_hours=100,
            ))
        # Employees
        for empno, name, status in [
            ("S4MB1", "멤버1", "재직"),
            ("S4MB2", "멤버2", "재직"),
            ("S4MB3", "퇴사자", "퇴사"),
        ]:
            if s.query(Employee).filter(Employee.empno == empno).first() is None:
                s.add(Employee(empno=empno, name=name, emp_status=status))
        # ProjectMembers
        s.query(ProjectMember).filter(ProjectMember.project_code == TEST_PROJECT_CODE).delete()
        s.add(ProjectMember(project_code=TEST_PROJECT_CODE, empno="S4MB1", name="멤버1", role="FLDT", grade="SA"))
        s.commit()
    finally:
        s.close()
    yield


def test_export_returns_xlsx_content_type(client, elpm_cookie):
    r = client.get(
        f"/api/v1/budget/projects/{TEST_PROJECT_CODE}/members/export",
        cookies=elpm_cookie,
    )
    assert r.status_code == 200
    assert "spreadsheetml.sheet" in r.headers.get("content-type", "")
    wb = load_workbook(io.BytesIO(r.content))
    ws = wb.active
    # Header row
    assert [c.value for c in ws[1]] == ["empno", "name", "role", "grade"]


def test_upload_replaces_fldt_members(client, elpm_cookie):
    wb = Workbook()
    ws = wb.active
    ws.append(["empno", "name", "role", "grade"])
    ws.append(["S4MB2", "멤버2", "FLDT", "M"])
    buf = io.BytesIO()
    wb.save(buf)
    buf.seek(0)

    r = client.post(
        f"/api/v1/budget/projects/{TEST_PROJECT_CODE}/members/upload",
        files={"file": ("members.xlsx", buf, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")},
        cookies=elpm_cookie,
    )
    assert r.status_code == 200, r.text
    body = r.json()
    assert body["imported_count"] == 1
    # Previous member S4MB1 should be gone (FLDT truncated)
    s = SessionLocal()
    try:
        remaining = s.query(ProjectMember).filter(
            ProjectMember.project_code == TEST_PROJECT_CODE,
            ProjectMember.role == "FLDT",
        ).all()
        assert len(remaining) == 1
        assert remaining[0].empno == "S4MB2"
    finally:
        s.close()


def test_upload_skips_inactive_empno(client, elpm_cookie):
    wb = Workbook()
    ws = wb.active
    ws.append(["empno", "name", "role", "grade"])
    ws.append(["S4MB3", "퇴사자", "FLDT", "SA"])
    buf = io.BytesIO()
    wb.save(buf)
    buf.seek(0)

    r = client.post(
        f"/api/v1/budget/projects/{TEST_PROJECT_CODE}/members/upload",
        files={"file": ("members.xlsx", buf, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")},
        cookies=elpm_cookie,
    )
    assert r.status_code == 200, r.text
    body = r.json()
    assert body["imported_count"] == 0
    assert any(s.get("reason") == "inactive" for s in body["skipped"])


def test_staff_cannot_upload_members(client, staff_cookie):
    wb = Workbook()
    ws = wb.active
    ws.append(["empno", "name", "role", "grade"])
    buf = io.BytesIO()
    wb.save(buf)
    buf.seek(0)
    r = client.post(
        f"/api/v1/budget/projects/{TEST_PROJECT_CODE}/members/upload",
        files={"file": ("members.xlsx", buf, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")},
        cookies=staff_cookie,
    )
    assert r.status_code in (401, 403)
```

### Step 3: 실행

```bash
cd backend && pytest tests/test_members_upload_export.py -v
```

Expected: 4 passed.

### Step 4: Commit

```bash
git add backend/app/api/v1/budget_input.py backend/tests/test_members_upload_export.py
git commit -m "feat(s4): Step 2 members Excel export/upload endpoints (#40)"
```

---

## Task 3: Frontend — Step 2 Excel 버튼 + 사번 검증 alert + Enter key (#40, #39, #54)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: EmployeeSearch Enter 핸들러 (#54)

Find `EmployeeSearch` component (around line 1667-1750). Locate the `<input>` tag (around line 1717). Add `onKeyDown`:

Before (example):
```tsx
<input
  value={query}
  onChange={(e) => setQuery(e.target.value)}
  onFocus={() => setOpen(true)}
  className="..."
/>
```

After:
```tsx
<input
  value={query}
  onChange={(e) => setQuery(e.target.value)}
  onFocus={() => setOpen(true)}
  onKeyDown={(e) => {
    if (e.key === "Enter" && results.length > 0) {
      e.preventDefault();
      onSelect(results[0]);
      setOpen(false);
      setQuery("");
    }
  }}
  className="..."
/>
```

Check actual state variable names — they may be `items` not `results`, and onSelect may have a different name. Use the actual names.

### Step 2: 퇴사자 입력 차단 (#39)

Find the member-add flow in Step 2. When `EmployeeSearch` onSelect is called with an employee whose `emp_status !== "재직"`:

```tsx
<EmployeeSearch
  onSelect={(emp) => {
    if (emp.emp_status && emp.emp_status !== "재직") {
      alert(`사번 ${emp.empno} 은(는) 현재 재직 중인 직원이 아닙니다. 퇴사/휴직 상태입니다.`);
      return;
    }
    // 기존 add 로직
    setMembers([...members, {...emp, role: "FLDT"}]);
  }}
/>
```

Since Task 1 filters the backend response to only active employees by default, the dropdown shouldn't even show inactive ones. But this client-side guard adds defense-in-depth for manual empno typing.

### Step 3: Step 2 상단에 Excel 버튼 2개 추가 (#40)

Find the Step 2 member list rendering section. Add above the table:

```tsx
<div className="flex items-center gap-2 mb-3">
  <button
    onClick={handleExportMembers}
    className="px-3 py-1.5 text-xs border border-pwc-gray-200 rounded-md hover:bg-pwc-gray-50 text-pwc-gray-900"
  >
    📥 Excel 다운로드
  </button>
  <label className="px-3 py-1.5 text-xs border border-pwc-gray-200 rounded-md hover:bg-pwc-gray-50 text-pwc-gray-900 cursor-pointer">
    📤 Excel 업로드
    <input type="file" accept=".xlsx" className="hidden" onChange={handleImportMembers} />
  </label>
</div>
```

Add the handlers at the component level:

```tsx
async function handleExportMembers() {
  const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:3001";
  window.location.href = `${API_BASE}/api/v1/budget/projects/${project.project_code}/members/export`;
}

async function handleImportMembers(e: React.ChangeEvent<HTMLInputElement>) {
  const file = e.target.files?.[0];
  if (!file) return;
  const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:3001";
  const fd = new FormData();
  fd.append("file", file);
  try {
    const r = await fetch(
      `${API_BASE}/api/v1/budget/projects/${project.project_code}/members/upload`,
      { method: "POST", body: fd, credentials: "include" }
    );
    if (!r.ok) {
      const d = await r.json().catch(() => ({ detail: "업로드 실패" }));
      alert(d.detail || "업로드 실패");
      return;
    }
    const data = await r.json();
    let msg = `${data.imported_count} 명 업로드 완료.`;
    if (data.skipped?.length > 0) {
      msg += `\n제외 ${data.skipped.length} 명:\n` +
        data.skipped.map((s: { empno?: string; reason: string }) =>
          `  - ${s.empno ?? "(no empno)"}: ${s.reason}`
        ).join("\n");
    }
    alert(msg);
    // 목록 재로드
    window.location.reload();
  } catch (err) {
    alert(`업로드 오류: ${err instanceof Error ? err.message : "알 수 없음"}`);
  }
  e.target.value = "";
}
```

**Note:** `window.location.reload()` is a pragmatic way to refresh members list. If the component has a `fetchMembers()` function, call that instead to avoid full page reload.

### Step 4: TypeScript + build

```bash
cd frontend && npx tsc --noEmit 2>&1 | grep page.tsx | head
cd frontend && npm run build 2>&1 | tail -5
```

Both must succeed.

### Step 5: Commit

```bash
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s4): Step2 Excel export/upload UI + Enter autocomplete + inactive guard (#39 #40 #54)"
```

---

## Task 4: #46 이전 단계 이동 시 state reset 조사 + 수정

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx` (if any bug found)

### Step 1: 재현 시도

```bash
cd /Users/jkim564/Documents/Programming/my_budget/.worktrees/s4-step2-ux/backend && uvicorn app.main:app --port 3001 > /tmp/s4-backend.log 2>&1 &
sleep 5
cd /Users/jkim564/Documents/Programming/my_budget/.worktrees/s4-step2-ux/frontend && NODE_ENV=production npm run build > /tmp/s4-build.log 2>&1
cd /Users/jkim564/Documents/Programming/my_budget/.worktrees/s4-step2-ux/frontend && (NODE_ENV=production npm run start -- --port 8001 > /tmp/s4-frontend.log 2>&1 &)
sleep 8
```

Manually navigate in browser:
1. `/login` → 170661 로그인
2. `/budget-input/new`
3. Step 1 에서 project_name, service_type, 클라이언트 일부 필드 채움
4. "다음" → Step 2
5. "이전" → Step 1 돌아왔을 때 필드 값이 보존되는지 확인

### Step 2: 원인 특정

```bash
cd frontend && grep -n "setStep\|step === 1\|step === 2\|key={step\|useEffect.*step" src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx | head -30
```

찾을 것:
- `useEffect` 의존성에 `step` 있는지 — 있으면 reset 로직 포함 여부
- `key={step}` 같이 Step 변경 시 컴포넌트가 재마운트되는지
- Step 별 서브컴포넌트가 자체 state 를 가지는지 (부모 state 아님)

### Step 3: 원인에 따라 수정

**Case A**: useEffect 가 step 변경 시 state 초기화 — dependency 제거 또는 `if (step === 1)` guard 추가

**Case B**: 서브컴포넌트 자체 state — 부모의 state 로 올려보내기 (lift state up)

**Case C**: key={step} 으로 강제 재마운트 — key 제거

**Case D**: 실제로는 reset 되지 않음 — 사용자 피드백은 다른 상황 (예: 페이지 새로고침, 에러 발생). 이 경우 Playwright 로 현 동작이 올바름을 회귀 테스트로 고정.

재현 결과에 따라 Step 4 로.

### Step 4: 수정 적용 후 테스트

재현이 확인되었다면 수정 후 다시 테스트. 재현 안 됨이면 별도 수정 불필요.

### Step 5: Commit (수정 있었을 경우에만)

```bash
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "fix(s4): preserve wizard state when navigating to previous step (#46)"
```

If the user-reported behavior could not be reproduced (no bug found), skip the commit and document in the final verification report that #46 could not be reproduced in current build — include in `Open Questions`.

---

## Task 5: Playwright E2E

**Files:**
- Create: `frontend/tests/task-s4-employees-search.spec.ts`
- Create: `frontend/tests/task-s4-members-upload.spec.ts`

### Step 1: Employees search API spec

Create `frontend/tests/task-s4-employees-search.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

const API = "http://localhost:3001/api/v1";
const EL = process.env.EL_EMPNO || "170661";

test.describe("S4 — Employees search (#39)", () => {
  test("returns emp_status field in each row", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    const r = await request.get(`${API}/budget/employees/search?q=`);
    expect(r.status()).toBe(200);
    const rows = await r.json();
    if (rows.length > 0) {
      expect(rows[0]).toHaveProperty("emp_status");
    }
  });

  test("default search returns only active employees", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    const r = await request.get(`${API}/budget/employees/search?q=`);
    const rows = await r.json();
    for (const row of rows) {
      expect(row.emp_status).toBe("재직");
    }
  });
});
```

### Step 2: Members upload/export spec

Create `frontend/tests/task-s4-members-upload.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

const API = "http://localhost:3001/api/v1";
const EL = process.env.EL_EMPNO || "170661";

test.describe("S4 — Members Excel upload/export (#40)", () => {
  test("export returns xlsx content-type", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    // Try any existing project from user's scope
    const ov = await request.get(`${API}/overview`);
    const body = await ov.json();
    const projects = body.projects || [];
    if (projects.length === 0) {
      test.skip();
      return;
    }
    const code = projects[0].project_code;
    const r = await request.get(
      `${API}/budget/projects/${code}/members/export`,
    );
    expect(r.status()).toBe(200);
    const ct = r.headers()["content-type"] || "";
    expect(ct).toContain("spreadsheetml");
  });

  test("export of nonexistent project returns 200 with empty workbook or 404", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    const r = await request.get(`${API}/budget/projects/NOEXIST_S4/members/export`);
    expect([200, 404]).toContain(r.status());
  });
});
```

### Step 3: Run specs

```bash
cd frontend && npx playwright test task-s4 --reporter=line 2>&1 | tail -8
```

Expected: 4 passed (or skip if no projects in scope).

### Step 4: Commit

```bash
git add frontend/tests/task-s4-employees-search.spec.ts frontend/tests/task-s4-members-upload.spec.ts
git commit -m "test(s4): Playwright E2E for employees search + members export"
```

---

## Task 6: Final verification

**Files:** 없음

### Step 1: Full pytest

```bash
cd backend && pytest -q 2>&1 | tail -3
```

Expected: baseline + 3 (Task 1) + 4 (Task 2) = +7.

### Step 2: Full Playwright

```bash
cd frontend && npx playwright test task-auth task-s1 task-s2 task-s3 task-s4 --reporter=line 2>&1 | tail -10
```

### Step 3: Commit log

```bash
cd /Users/jkim564/Documents/Programming/my_budget/.worktrees/s4-step2-ux && git log main..HEAD --oneline
```

Expected: 4-5 commits.

### Step 4: Feedback mapping

| # | Commit |
|---|---|
| #39 | Task 1 backend + Task 3 frontend alert |
| #40 | Task 2 backend + Task 3 UI buttons |
| #46 | Task 4 (조건부 — 재현 시 fix) |
| #54 | Task 3 Enter onKeyDown |

### Step 5: Manual verification (사용자 인계)

- 구성원 검색 시 퇴사자 미노출 (#39)
- Step 2 Excel 다운로드 버튼 → xlsx 다운 (#40)
- Step 2 Excel 업로드 → 구성원 목록 갱신 (#40)
- Step 2 에서 이름 입력 후 Enter → 첫 결과 선택 (#54)
- Step 1 → Step 2 → 이전 → Step 1 필드 보존 확인 (#46 재현 시도)

---

## 완료 기준

- [ ] Task 1-5 완료
- [ ] pytest baseline+7 passed
- [ ] S0~S4 Playwright 회귀 없음
- [ ] 4 feedback items 매핑 완료 (#46 은 재현 여부에 따라)
