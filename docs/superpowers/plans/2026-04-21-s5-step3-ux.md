# S5 — Step 3 Time Budget UX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자 피드백 #19/#20/#21/#22/#41/#43/#44/#52 대응. Step 3 Time Budget 의 입력 검증·임시저장 보존·초기화 버튼·등록완료 누락 안내·월/분기 토글·Excel 업·다운로드·비감사 회계연도 동적 MONTHS.

**Architecture:**
- **백엔드 1 파일:** `budget_input.py` — template export/upload 엔드포인트 추가
- **프론트 2 파일:** `budget-input/[project_code]/page.tsx` — toolbar/validation/initialize/quarterly toggle, `lib/budget-constants.ts` — fiscal_start 기반 MONTHS 동적 생성
- **테스트:** pytest 1 파일, Playwright 1 spec

**Tech Stack:** FastAPI, openpyxl, Next.js 16, React, Playwright, pytest.

**Spec:** [docs/superpowers/specs/2026-04-21-s5-step3-ux-design.md](../specs/2026-04-21-s5-step3-ux-design.md)

---

## Task 1: NumberField max + month cell 검증 강화 (#41)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: NumberField 에 `max` prop 추가

NumberField 정의 (around line 21-81). 추가:

Before:
```tsx
interface NumberFieldProps {
  label: string;
  value?: number;
  onChange?: (v: number) => void;
  readOnly?: boolean;
  step?: number;
  min?: number;
  allowNegative?: boolean;
}
```

After:
```tsx
interface NumberFieldProps {
  label?: string;
  value?: number;
  onChange?: (v: number) => void;
  readOnly?: boolean;
  step?: number;
  min?: number;
  max?: number;
  allowNegative?: boolean;
}
```

내부 onChange 에 max 적용:

```tsx
onChange={(e) => {
  let v = parseFloat(e.target.value);
  if (Number.isNaN(v)) v = 0;
  if (!allowNegative && v < 0) v = 0;
  if (typeof min === "number" && v < min) v = min;
  if (typeof max === "number" && v > max) v = max;
  onChange?.(v);
}}
```

`<input>` element 에도 `max={max}` 속성 전달.

### Step 2: Step 3 month cell 에 max=300 + step=0.25 적용

Find month cell input (around lines 2675-2700). Update each month cell:

Before:
```tsx
<NumberField
  value={row.hours[m]}
  onChange={(v) => updateMonthHours(rowIdx, m, v)}
  min={0}
  step={0.5}
/>
```

After:
```tsx
<NumberField
  value={row.hours[m]}
  onChange={(v) => updateMonthHours(rowIdx, m, v)}
  min={0}
  max={300}
  step={0.25}
/>
```

### Step 3: Build check

```bash
cd frontend && npm run build 2>&1 | tail -3
```

### Step 4: Commit

```bash
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s5): NumberField max prop + month cell max=300 step=0.25 (#41)"
```

---

## Task 2: 입력 초기화 버튼 (#44)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: Toolbar 에 초기화 버튼 추가

Find Step 3 toolbar (lines 2378-2412). Add a new button after AI buttons but before "+ 행 추가":

```tsx
<button
  type="button"
  onClick={() => {
    if (!confirm("Time Budget 의 모든 입력값을 초기화 합니다. 계속하시겠습니까?")) return;
    setTemplateRows((prev) => prev.map((r) => ({
      ...r,
      enabled: false,
      empno: "",
      hours: Object.fromEntries(MONTHS.map((m) => [m, 0])) as Record<string, number>,
    })));
  }}
  className="px-3 py-1.5 text-xs border border-pwc-gray-200 rounded-md hover:bg-pwc-gray-50 text-pwc-gray-900"
>
  🔄 초기화
</button>
```

Match existing toolbar button className. Order: AI 추천 / AI 검증 / 🔄 초기화 / + 행 추가.

### Step 2: Build + commit

```bash
cd frontend && npm run build 2>&1 | tail -3
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s5): Step3 toolbar 초기화 button (#44)"
```

---

## Task 3: 등록완료 시 누락 안내 (#52)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: saveAll 함수에 validation 추가

Find `saveAll` function (around lines 525-548). At the very top of the function, BEFORE the existing template save logic, add a validation block that runs when `status === "작성완료"`:

```tsx
async function saveAll(status: string) {
  // ... existing setup

  // S5 #52: 등록완료 시 누락 안내
  if (status === "작성완료") {
    const enabledRows = templateRows.filter((r) => r.enabled);
    const errors: string[] = [];

    const noEmpno = enabledRows.filter((r) => !r.empno);
    if (noEmpno.length > 0) {
      errors.push(
        `담당자 미지정 ${noEmpno.length}건:\n` +
          noEmpno
            .slice(0, 5)
            .map((r) => `  - ${r.budget_unit ?? r.unit_name}`)
            .join("\n") +
          (noEmpno.length > 5 ? `\n  ...외 ${noEmpno.length - 5}건` : "")
      );
    }

    const noHours = enabledRows.filter((r) =>
      Object.values(r.hours ?? {}).every((h) => !h || h === 0)
    );
    if (noHours.length > 0) {
      errors.push(`시간 미입력 ${noHours.length}건`);
    }

    const totalSum = enabledRows.reduce(
      (s, r) => s + Object.values(r.hours ?? {}).reduce((a, b) => a + (b || 0), 0),
      0
    );
    if (Math.abs(totalSum - etControllable) > 0.01) {
      errors.push(
        `시간 합계 ${totalSum.toLocaleString()} ≠ ET 잔여시간 ${etControllable.toLocaleString()} (차이: ${(totalSum - etControllable).toFixed(1)}h)`
      );
    }

    if (errors.length > 0) {
      alert("등록완료 전 확인이 필요합니다:\n\n" + errors.join("\n\n"));
      return;
    }
  }

  // ... continue with existing save logic
}
```

**Important:** Find the actual saveAll signature and parameter names. If `status` isn't a parameter, locate where it's set (e.g., from a state variable). Adapt accordingly.

### Step 2: Build + commit

```bash
cd frontend && npm run build 2>&1 | tail -3
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s5): saveAll validates 누락 fields when 작성완료 (#52)"
```

---

## Task 4: 임시저장 시 disabled 행 보존 (#22)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: saveStep3 / saveAll 에서 enabled 필터 변경

Find `saveStep3` (around line 454-455) and `saveAll` template-save block (line 525). Currently:

```tsx
const enabledRows = templateRows.filter((r) => r.enabled);
// POST { rows: enabledRows }
```

**Change**: also include rows where any hours value > 0:

```tsx
const rowsToSave = templateRows.filter((r) => {
  if (r.enabled) return true;
  const hasHours = Object.values(r.hours ?? {}).some((h) => h && h > 0);
  return hasHours;
});
// POST { rows: rowsToSave }
```

This keeps 사용자가 enabled=true 토글한 rows AND 시간 입력 있는 rows. enabled=false + 0 hours 는 master units 리스트에서 매번 새로 생성되므로 손실 없음.

### Step 2: Load 로직 확인

Around line 314, the `useEffect` that loads templateRows from `/template`. Verify that any DB row sets enabled=true. The current behavior should be preserved — DB row 가 있으면 enabled=true.

### Step 3: Build + commit

```bash
cd frontend && npm run build 2>&1 | tail -3
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "fix(s5): Step3 임시저장 시 enabled 행 보존 (#22)"
```

---

## Task 5: Quarter view toggle (#19)

**Files:**
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: viewMode state + toggle UI

At Step 3 component level, add:

```tsx
const [viewMode, setViewMode] = useState<"month" | "quarter">("month");
```

Toolbar 에 toggle:

```tsx
<div className="flex items-center gap-1 text-xs">
  <button
    onClick={() => setViewMode("month")}
    className={`px-2 py-1 rounded ${viewMode === "month" ? "bg-pwc-orange text-white" : "border border-pwc-gray-200 text-pwc-gray-600"}`}
  >월</button>
  <button
    onClick={() => setViewMode("quarter")}
    className={`px-2 py-1 rounded ${viewMode === "quarter" ? "bg-pwc-orange text-white" : "border border-pwc-gray-200 text-pwc-gray-600"}`}
  >분기</button>
</div>
```

### Step 2: 분기 헬퍼

Add at component scope:

```tsx
const QUARTERS = useMemo(() => {
  // group MONTHS by 3
  const out: { label: string; months: string[] }[] = [];
  for (let i = 0; i < MONTHS.length; i += 3) {
    const slice = MONTHS.slice(i, i + 3);
    const labelStart = parseInt(slice[0].slice(5), 10);
    const labelEnd = parseInt(slice[slice.length - 1].slice(5), 10);
    const qIdx = i / 3 + 1;
    out.push({ label: `${qIdx}Q (${labelStart}-${labelEnd}월)`, months: slice });
  }
  return out;
}, [MONTHS]);

function quarterSum(row: TemplateRow, q: { months: string[] }): number {
  return q.months.reduce((s, m) => s + (row.hours?.[m] ?? 0), 0);
}
```

### Step 3: Render columns conditionally

In the table thead/tbody, branch on `viewMode`:

```tsx
{viewMode === "month" ? (
  MONTH_LABELS.map((m, mi) => <th key={`${mi}-${m}`}>{m}</th>)
) : (
  QUARTERS.map((q) => <th key={q.label}>{q.label}</th>)
)}
```

In tbody cells:

```tsx
{viewMode === "month" ? (
  MONTHS.map((m, mi) => (
    <td key={`${mi}-${m}`}>
      <NumberField
        value={row.hours[m]}
        onChange={(v) => updateMonthHours(rowIdx, m, v)}
        min={0}
        max={300}
        step={0.25}
      />
    </td>
  ))
) : (
  QUARTERS.map((q) => (
    <td key={q.label} className="text-right text-xs text-pwc-gray-700">
      {quarterSum(row, q).toLocaleString("ko-KR")}
    </td>
  ))
)}
```

### Step 4: Build + commit

```bash
cd frontend && npm run build 2>&1 | tail -3
git add frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s5): Step3 월/분기 view toggle (#19)"
```

---

## Task 6: Template Excel export/upload backend (#20, #43)

**Files:**
- Modify: `backend/app/api/v1/budget_input.py`
- Create: `backend/tests/test_template_upload_export.py`

### Step 1: Failing test

Create `backend/tests/test_template_upload_export.py`:

```python
"""Tests for Step 3 template Excel export/upload (#20, #43)."""
import io

import pytest
from openpyxl import Workbook, load_workbook

from app.db.session import SessionLocal
from app.models.project import Project
from app.models.budget import BudgetDetail


TEST_PJ = "S5_TEST_TPL"


@pytest.fixture(autouse=True)
def _seed():
    s = SessionLocal()
    try:
        if s.query(Project).filter(Project.project_code == TEST_PJ).first() is None:
            s.add(Project(
                project_code=TEST_PJ,
                project_name="S5 Template Test",
                el_empno="170661", pm_empno="170661",
                contract_hours=100,
            ))
        s.query(BudgetDetail).filter(BudgetDetail.project_code == TEST_PJ).delete()
        s.add(BudgetDetail(
            project_code=TEST_PJ,
            budget_category="자산",
            budget_unit="매출채권-일반",
            empno="170661", emp_name="최성우",
            year_month="2025-04",
            budget_hours=10.0,
        ))
        s.commit()
    finally:
        s.close()
    yield


def test_template_export_xlsx(client, elpm_cookie):
    r = client.get(
        f"/api/v1/budget/projects/{TEST_PJ}/template/export",
        cookies=elpm_cookie,
    )
    assert r.status_code == 200, r.text
    assert "spreadsheetml.sheet" in r.headers.get("content-type", "")
    wb = load_workbook(io.BytesIO(r.content))
    ws = wb.active
    headers = [c.value for c in ws[1]]
    # Header should include the 5 base + 12 month columns
    assert "budget_category" in headers
    assert "budget_unit" in headers
    assert "empno" in headers
    assert any("월" in (h or "") or "-" in (h or "") for h in headers if h)


def test_template_upload_replaces_rows(client, elpm_cookie):
    wb = Workbook()
    ws = wb.active
    # Match 5 base + at least one month column ("2025-04")
    ws.append(["budget_category", "budget_unit", "empno", "name", "grade", "2025-04"])
    ws.append(["자산", "매출채권-일반", "170661", "최성우", "EL", 25.0])
    buf = io.BytesIO()
    wb.save(buf)
    buf.seek(0)

    r = client.post(
        f"/api/v1/budget/projects/{TEST_PJ}/template/upload",
        files={"file": ("template.xlsx", buf, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")},
        cookies=elpm_cookie,
    )
    assert r.status_code == 200, r.text
    body = r.json()
    assert body["imported_count"] >= 1


def test_staff_cannot_upload_template(client, staff_cookie):
    wb = Workbook()
    ws = wb.active
    ws.append(["budget_category", "budget_unit", "empno", "name", "grade", "2025-04"])
    buf = io.BytesIO()
    wb.save(buf)
    buf.seek(0)
    r = client.post(
        f"/api/v1/budget/projects/{TEST_PJ}/template/upload",
        files={"file": ("template.xlsx", buf, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")},
        cookies=staff_cookie,
    )
    assert r.status_code in (401, 403)
```

### Step 2: Run — expect failures

```bash
cd backend && pytest tests/test_template_upload_export.py -v
```

Expected: 404 because endpoints don't exist.

### Step 3: Add endpoints

In `backend/app/api/v1/budget_input.py`, after the members upload (around line 940+ or inside Step 2 section), add a new "Step 3 template" section. Place the export/upload BEFORE any plain `/template` GET that takes `{project_code}` (FastAPI route ordering).

```python
# ── Step 3 Template Excel Export / Upload ──────────────────


@router.get("/projects/{project_code}/template/export")
def export_project_template(
    project_code: str,
    user: dict = Depends(require_login),
    db: Session = Depends(get_db),
):
    """Step 3 Time Budget Excel 다운로드."""
    from openpyxl import Workbook
    from app.models.budget import BudgetDetail

    rows = (
        db.query(BudgetDetail)
        .filter(BudgetDetail.project_code == project_code)
        .order_by(BudgetDetail.budget_category, BudgetDetail.budget_unit, BudgetDetail.empno, BudgetDetail.year_month)
        .all()
    )

    # Group by (category, unit, empno) — pivot months as columns
    months: list[str] = sorted({r.year_month for r in rows if r.year_month})
    pivot: dict[tuple, dict] = {}
    name_by_empno: dict[str, str] = {}
    grade_by_empno: dict[str, str] = {}
    for r in rows:
        key = (r.budget_category or "", r.budget_unit or "", r.empno or "")
        pivot.setdefault(key, {})
        pivot[key][r.year_month] = (pivot[key].get(r.year_month) or 0) + (r.budget_hours or 0)
        if r.emp_name:
            name_by_empno[r.empno or ""] = r.emp_name
        if r.grade:
            grade_by_empno[r.empno or ""] = r.grade

    wb = Workbook()
    ws = wb.active
    ws.title = "Time Budget"
    headers = ["budget_category", "budget_unit", "empno", "name", "grade"] + months
    ws.append(headers)
    for (cat, unit, emp), month_hours in pivot.items():
        row_data = [cat, unit, emp, name_by_empno.get(emp, ""), grade_by_empno.get(emp, "")]
        for m in months:
            row_data.append(month_hours.get(m, 0))
        ws.append(row_data)

    buf = io.BytesIO()
    wb.save(buf)
    buf.seek(0)
    return StreamingResponse(
        buf,
        media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": f'attachment; filename="template_{project_code}.xlsx"'},
    )


@router.post("/projects/{project_code}/template/upload")
async def upload_project_template(
    project_code: str,
    file: UploadFile = File(...),
    user: dict = Depends(require_elpm),
    db: Session = Depends(get_db),
):
    """Step 3 Time Budget Excel 업로드 — 기존 budget_details truncate+insert."""
    from openpyxl import load_workbook
    from app.models.budget import BudgetDetail
    from app.api.deps import assert_can_modify_project

    assert_can_modify_project(db, user, project_code)

    content = await file.read()
    wb = load_workbook(io.BytesIO(content), data_only=True)
    ws = wb.active

    rows = list(ws.iter_rows(values_only=True))
    if not rows:
        return {"imported_count": 0, "skipped": [{"reason": "empty"}]}

    headers = [str(h) if h is not None else "" for h in rows[0]]
    base_cols = {"budget_category": -1, "budget_unit": -1, "empno": -1, "name": -1, "grade": -1}
    month_cols: list[tuple[int, str]] = []
    for idx, h in enumerate(headers):
        h_clean = h.strip()
        if h_clean in base_cols:
            base_cols[h_clean] = idx
        elif "-" in h_clean and len(h_clean) >= 7:  # "YYYY-MM"
            month_cols.append((idx, h_clean))

    if base_cols["budget_unit"] == -1:
        from fastapi import HTTPException
        raise HTTPException(status_code=400, detail="헤더에 budget_unit 컬럼이 필요합니다.")

    # Truncate existing
    db.query(BudgetDetail).filter(BudgetDetail.project_code == project_code).delete(synchronize_session=False)
    db.commit()

    imported = 0
    skipped: list[dict] = []
    for ridx, row in enumerate(rows[1:], start=2):
        if not row or not any(c is not None for c in row):
            continue
        cat = str(row[base_cols["budget_category"]]).strip() if base_cols["budget_category"] >= 0 and row[base_cols["budget_category"]] else ""
        unit = str(row[base_cols["budget_unit"]]).strip() if row[base_cols["budget_unit"]] else ""
        empno = str(row[base_cols["empno"]]).strip() if base_cols["empno"] >= 0 and row[base_cols["empno"]] else ""
        name = str(row[base_cols["name"]]).strip() if base_cols["name"] >= 0 and row[base_cols["name"]] else ""
        grade = str(row[base_cols["grade"]]).strip() if base_cols["grade"] >= 0 and row[base_cols["grade"]] else ""
        if not unit:
            skipped.append({"row": ridx, "reason": "budget_unit missing"})
            continue
        for col_idx, year_month in month_cols:
            cell = row[col_idx] if col_idx < len(row) else None
            try:
                hours = float(cell or 0)
            except (TypeError, ValueError):
                hours = 0
            if hours == 0:
                continue
            db.add(BudgetDetail(
                project_code=project_code,
                budget_category=cat,
                budget_unit=unit,
                empno=empno,
                emp_name=name,
                grade=grade,
                year_month=year_month,
                budget_hours=hours,
            ))
            imported += 1

    db.commit()
    return {"imported_count": imported, "skipped": skipped}
```

### Step 4: Test pass

```bash
cd backend && pytest tests/test_template_upload_export.py -v
```

Expected: 3 passed.

### Step 5: Full pytest

```bash
cd backend && pytest -q 2>&1 | tail -3
```

### Step 6: Commit

```bash
git add backend/app/api/v1/budget_input.py backend/tests/test_template_upload_export.py
git commit -m "feat(s5): Step3 template Excel export/upload (#20 #43)"
```

---

## Task 7: Frontend Step 3 Excel buttons + dynamic MONTHS (#20, #43, #21)

**Files:**
- Modify: `frontend/src/lib/budget-constants.ts`
- Modify: `frontend/src/app/(dashboard)/budget-input/[project_code]/page.tsx`

### Step 1: Add `generateMonths` helper to `budget-constants.ts`

Add to `frontend/src/lib/budget-constants.ts`:

```ts
export function generateMonths(fiscalStartIso: string | null | undefined): string[] {
  let startMonth = 4;
  let startYear = new Date().getFullYear();
  if (fiscalStartIso) {
    const d = new Date(fiscalStartIso);
    if (!isNaN(d.getTime())) {
      startMonth = d.getMonth() + 1;
      startYear = d.getFullYear();
    }
  }
  const months: string[] = [];
  for (let i = 0; i < 12; i++) {
    const m = ((startMonth - 1 + i) % 12) + 1;
    const y = startYear + Math.floor((startMonth - 1 + i) / 12);
    months.push(`${y}-${String(m).padStart(2, "0")}`);
  }
  return months;
}

export function generateMonthLabels(months: string[]): string[] {
  return months.map((m) => `${parseInt(m.slice(5), 10)}월`);
}
```

Keep the existing `MONTHS` and `MONTH_LABELS` exports as default (4월~3월 fixed for backward compat).

### Step 2: page.tsx — useMemo dynamic MONTHS

Find imports:
```tsx
import { MONTHS, MONTH_LABELS } from "@/lib/budget-constants";
```

Change to:
```tsx
import {
  MONTHS as DEFAULT_MONTHS,
  MONTH_LABELS as DEFAULT_MONTH_LABELS,
  generateMonths,
  generateMonthLabels,
} from "@/lib/budget-constants";
```

Inside `BudgetWizardPage` (or wherever `project` state is in scope):

```tsx
const MONTHS = useMemo(
  () => (project.fiscal_start ? generateMonths(project.fiscal_start) : DEFAULT_MONTHS),
  [project.fiscal_start]
);
const MONTH_LABELS = useMemo(() => generateMonthLabels(MONTHS), [MONTHS]);
```

If `MONTHS` is referenced inside sub-components like `Step3Section`, pass them as props OR move `useMemo` to the sub-component using `project.fiscal_start` from props.

### Step 3: Step 3 toolbar — Excel 다운로드/업로드 버튼

Add buttons next to other Step 3 toolbar items (after "🔄 초기화"):

```tsx
<button
  type="button"
  onClick={handleExportTemplate}
  className="px-3 py-1.5 text-xs border border-pwc-gray-200 rounded-md hover:bg-pwc-gray-50 text-pwc-gray-900"
>
  📥 Excel 다운로드
</button>
<label className="px-3 py-1.5 text-xs border border-pwc-gray-200 rounded-md hover:bg-pwc-gray-50 text-pwc-gray-900 cursor-pointer">
  📤 Excel 업로드
  <input
    type="file"
    accept=".xlsx"
    className="hidden"
    onChange={handleImportTemplate}
  />
</label>
```

Handlers (alongside Step 2's similar handlers from S4):

```tsx
async function handleExportTemplate() {
  const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:3001";
  const res = await fetch(
    `${API_BASE}/api/v1/budget/projects/${project.project_code}/template/export`,
    { credentials: "include" }
  );
  if (!res.ok) {
    alert("다운로드 실패");
    return;
  }
  const blob = await res.blob();
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = `template_${project.project_code}.xlsx`;
  a.click();
  URL.revokeObjectURL(url);
}

async function handleImportTemplate(e: React.ChangeEvent<HTMLInputElement>) {
  const file = e.target.files?.[0];
  if (!file) return;
  if (!confirm("기존 Time Budget 데이터가 모두 교체됩니다. 계속하시겠습니까?")) {
    e.target.value = "";
    return;
  }
  const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:3001";
  const fd = new FormData();
  fd.append("file", file);
  try {
    const r = await fetch(
      `${API_BASE}/api/v1/budget/projects/${project.project_code}/template/upload`,
      { method: "POST", body: fd, credentials: "include" }
    );
    if (!r.ok) {
      const d = await r.json().catch(() => ({ detail: "업로드 실패" }));
      alert(d.detail || "업로드 실패");
      return;
    }
    const data = await r.json();
    alert(`${data.imported_count}건 업로드 완료. 페이지를 다시 로드합니다.`);
    // Reload template only (not full page) — call the existing fetchTemplate or similar
    if (typeof window !== "undefined") window.location.search = window.location.search;  // no-op fallback
  } catch (err) {
    alert(`업로드 오류: ${err instanceof Error ? err.message : "알 수 없음"}`);
  }
  e.target.value = "";
}
```

**Note:** If page has a `fetchTemplate()` function, call it instead of reload trick. Keeps wizard step preserved (per S4 fix).

### Step 4: Build + commit

```bash
cd frontend && npx tsc --noEmit 2>&1 | grep page.tsx | head
cd frontend && npm run build 2>&1 | tail -5
git add frontend/src/lib/budget-constants.ts frontend/src/app/\(dashboard\)/budget-input/\[project_code\]/page.tsx
git commit -m "feat(s5): Step3 Excel buttons + fiscal_start-based dynamic MONTHS (#20 #21 #43)"
```

---

## Task 8: Playwright E2E

**Files:**
- Create: `frontend/tests/task-s5-template-export.spec.ts`

### Step 1: Spec

Create `frontend/tests/task-s5-template-export.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

const API = "http://localhost:3001/api/v1";
const EL = process.env.EL_EMPNO || "170661";

test.describe("S5 — Template Excel export (#20 #43)", () => {
  test("template/export returns xlsx for accessible project", async ({ request }) => {
    await request.post(`${API}/auth/login`, { data: { empno: EL } });
    const ov = await request.get(`${API}/overview`);
    const body = await ov.json();
    const projects = body.projects || [];
    if (projects.length === 0) {
      test.skip();
      return;
    }
    const code = projects[0].project_code;
    const r = await request.get(
      `${API}/budget/projects/${code}/template/export`,
    );
    expect(r.status()).toBe(200);
    const ct = r.headers()["content-type"] || "";
    expect(ct).toContain("spreadsheetml");
  });
});
```

### Step 2: Run + commit

```bash
cd frontend && npx playwright test task-s5 --reporter=line 2>&1 | tail -5
git add frontend/tests/task-s5-template-export.spec.ts
git commit -m "test(s5): Playwright for template export"
```

---

## Task 9: Final verification

### Step 1: Full pytest + Playwright

```bash
cd backend && pytest -q 2>&1 | tail -3
cd frontend && npx playwright test task-auth task-s1 task-s2 task-s3 task-s4 task-s5 --reporter=line 2>&1 | tail -8
```

### Step 2: Branch commits + manual checklist

```bash
cd /Users/jkim564/Documents/Programming/my_budget/.worktrees/s5-step3-ux && git log main..HEAD --oneline
```

### Step 3: Manual verification (사용자 인계)

- Step 3 month cell 음수/300 초과 거부
- 초기화 버튼 → 모든 행 reset
- 등록완료 시 누락 사항 alert
- 임시저장 → 새로고침 → V 표시 + 시간 행 보존
- 월/분기 toggle 동작
- Excel 다운로드/업로드 동작
- 비감사 프로젝트 (fiscal_start ≠ 4월) → MONTHS 동적 변경

---

## 완료 기준

- [ ] Task 1-8 완료
- [ ] pytest baseline+3 (Task 6) passed
- [ ] Playwright S5 spec pass
- [ ] 7 feedback items 매핑 완료
